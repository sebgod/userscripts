<!--pas
uses MandelbrotSet
    ,MimeType
    ;

var mime :=  TMimeType.PNG;
var params := JSON.Parse(ParseRison(WebRequest.QueryString));
var size : integer; var z : TComplex; var zoom : float;
MandelbrotParams(params, size, z, zoom);

WebResponse.ContentType := MimeToString(mime);
WebResponse.ContentData := MandelbrotAsImage(mime, size, z, zoom);
?-->